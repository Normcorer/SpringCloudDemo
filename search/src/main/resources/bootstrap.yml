spring:
  cloud:
    config:
      uri: http://localhost:10000/
      profile: dev
  application:
    name: searchService-config
management:
  endpoints:
    web:
      exposure:
        include:
          - refresh



